<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.softdroom.truegospelheronetwork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Replicaofmaterial">
        <activity android:name=".MainActivity"
            android:theme="@style/Replicaofmaterial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Replicaofmaterial" android:name=".Authentication.LoginActivity" />
        <activity android:theme="@style/Replicaofmaterial" android:name=".Authentication.SignupActivity"></activity>
    </application>

</manifest>